<!DOCTYPE html>
<html>
	<head>
		<title> Add New Record </title>
		<h1 style = "text-align: center;"> Add a Record </h1>
	</head>
	<body style = "text-align: center;">
		<div class = "container" style = "display: inline-block; margin-left: auto; margin-right: auto; text-align: left;">
			<div class = "col-md-6 col-md-offset-3" style = "margin-top: 70px;">
				<%= form_for(@student) do |f| %>
					<div class = "form-group">
						<%= f.label :name %>
						<%= f.text_field :name %>
					</div>
					<div class = "form-group">
						<%= f.label :age %>
						<%= f.number_field :age %>
					</div>
					<%= f.submit "Add a record", class: "btn btn-primary" %>
				<% end %>
			</div>
		</div>
	</body>
</html>